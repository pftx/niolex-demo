<?xml version="1.0" encoding="UTF-8" ?>
<features xmlns="http://ff4j.org/schema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://ff4j.org/schema http://ff4j.org/schema/ff4j-1.2.0.xsd">

	<!-- Will be "ON" if enable is set as true -->
	<feature uid="hello" enable="true" description="This is my firts feature" />

	<feature uid="sayHello" description="my first feature" enable="true">
		<security>
			<role name="admin" />
		</security>
	</feature>

	<feature uid="lets.work" enable="true" description="some desc">
		<security>
			<role name="beta" />
			<role name="user" />
		</security>
		<flipstrategy class="org.ff4j.hello.strategy.OfficeHoursFlippingStrategy">
			<param name="startHour">9</param>
			<param name="endHour">18</param>
		</flipstrategy>
	</feature>

	<feature uid="sayGoodBye" description="null" enable="true">
		<security>
			<role name="beta" />
			<role name="user" />
		</security>
	</feature>

	<feature uid="language-french" enable="false" />

	<!-- Will be "ON" only if : (1) Enable is set as true (2) A security provider is 
		defined (3) The current logged user has the correct permissions. -->
	<feature uid="mySecuredFeature" enable="true">
		<security>
			<role name="USER" />
			<role name="ADMIN" />
		</security>
	</feature>

	<!-- Will be "ON" only if (1) Enable is set as true (2) Strategy predicate is true 
		(here current > releaseDate) -->
	<feature uid="myFutureFeature" enable="true">
		<flipstrategy class="org.ff4j.strategy.ReleaseDateFlipStrategy">
			<param name="releaseDate" value="2020-07-14-14:00" />
		</flipstrategy>
	</feature>

	<!-- Features can be grouped to be toggled in the same time -->
	<feature-group name="sprint_3">
		<feature uid="userStory3_1" enable="false" />
		<feature uid="userStory3_2" enable="false" />
	</feature-group>

</features>